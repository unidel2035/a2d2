<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title>Регистрация - A2D2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.14/dist/full.min.css" rel="stylesheet" type="text/css" />
  </head>

  <body class="bg-gradient-to-br from-primary/10 to-secondary/10 min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-md">
      <!-- Logo and Title -->
      <div class="text-center mb-8">
        <%= link_to root_path, class: "inline-block" do %>
          <h1 class="text-5xl font-bold bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent mb-2">A2D2</h1>
        <% end %>
        <p class="text-lg text-base-content/70">Создайте аккаунт для начала работы</p>
      </div>

      <!-- Registration Card -->
      <div class="card bg-base-100 shadow-2xl">
        <div class="card-body">
          <h2 class="card-title text-2xl mb-6 justify-center">Регистрация</h2>

          <%= form_with model: @user, url: signup_path, class: "space-y-4" do |form| %>
            <% if @user.errors.any? %>
              <div class="alert alert-error">
                <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <div>
                  <h3 class="font-bold">Ошибки при регистрации:</h3>
                  <ul class="list-disc list-inside">
                    <% @user.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            <% end %>

            <!-- Name Field -->
            <div class="form-control">
              <%= form.label :name, "Имя", class: "label" %>
              <%= form.text_field :name, class: "input input-bordered w-full", placeholder: "Введите ваше имя", autofocus: true, required: true %>
            </div>

            <!-- Email Field -->
            <div class="form-control">
              <%= form.label :email, "Email", class: "label" %>
              <%= form.email_field :email, class: "input input-bordered w-full", placeholder: "your@email.com", required: true %>
            </div>

            <!-- Password Field -->
            <div class="form-control">
              <%= form.label :password, "Пароль", class: "label" %>
              <%= form.password_field :password, class: "input input-bordered w-full", placeholder: "Минимум 6 символов", required: true %>
            </div>

            <!-- Password Confirmation Field -->
            <div class="form-control">
              <%= form.label :password_confirmation, "Подтверждение пароля", class: "label" %>
              <%= form.password_field :password_confirmation, class: "input input-bordered w-full", placeholder: "Повторите пароль", required: true %>
            </div>

            <!-- Submit Button -->
            <div class="form-control mt-6">
              <%= form.submit "Зарегистрироваться", class: "btn btn-primary btn-block btn-lg" %>
            </div>
          <% end %>

          <!-- Divider -->
          <div class="divider">или</div>

          <!-- Login Link -->
          <div class="text-center">
            <p class="text-base-content/70">
              Уже есть аккаунт?
              <%= link_to "Войти", login_path, class: "link link-primary font-semibold" %>
            </p>
          </div>
        </div>
      </div>

      <!-- Back to Home -->
      <div class="text-center mt-6">
        <%= link_to root_path, class: "link link-neutral" do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline-block mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
          </svg>
          Вернуться на главную
        <% end %>
      </div>
    </div>
  </body>
</html>

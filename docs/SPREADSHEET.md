# Табличный редактор / Spreadsheet Editor

## Обзор

Полнофункциональный табличный редактор с возможностями, вдохновленными Coda.io, Notion Tables и Google Sheets. Поддерживает динамические колонки, формулы, совместное редактирование и красивый современный дизайн.

## Возможности

### ✅ Основные функции

- **Множественные таблицы**: Создавайте неограниченное количество таблиц (spreadsheets)
- **Множественные листы**: Каждая таблица может содержать несколько листов (sheets)
- **Динамические колонки**: Добавляйте/удаляйте колонки на лету
- **Типизированные данные**: Поддержка text, number, integer, decimal, boolean, date, datetime
- **Формульный язык**: Мощная система формул с функциями и ссылками на ячейки
- **Совместное редактирование**: Real-time обновления через ActionCable (WebSockets)
- **Права доступа**: Система прав (view, edit, admin) для коллабораторов
- **Красивый UI**: Современный интерфейс с keyboard navigation

### 📊 Типы данных

- `text` - Текстовые данные
- `number` / `decimal` - Числа с плавающей точкой
- `integer` - Целые числа
- `boolean` - true/false
- `date` - Даты
- `datetime` - Дата и время

### 🧮 Формульный язык

#### Базовая арифметика

```
=10 + 5      # Сложение
=20 - 8      # Вычитание
=5 * 4       # Умножение
=100 / 5     # Деление
```

#### Ссылки на ячейки

```
=A1          # Значение ячейки A1
=B2 + C2     # Сумма значений двух ячеек
=A1 * 2      # Умножение значения ячейки на 2
```

#### Диапазоны ячеек

```
=A1:A10      # Диапазон от A1 до A10 (одна колонка)
```

#### Функции

##### SUM - Сумма значений
```
=SUM(A1:A10)         # Сумма всех значений в диапазоне
=SUM(10, 20, 30)     # Сумма чисел
```

##### AVG - Среднее значение
```
=AVG(A1:A10)         # Среднее арифметическое
```

##### COUNT - Подсчет значений
```
=COUNT(A1:A10)       # Количество непустых ячеек
```

##### MIN - Минимальное значение
```
=MIN(A1:A10)         # Наименьшее значение в диапазоне
```

##### MAX - Максимальное значение
```
=MAX(A1:A10)         # Наибольшее значение в диапазоне
```

##### IF - Условие
```
=IF(A1 > 10, "Больше", "Меньше")
```

#### Строковые литералы
```
="Привет, мир!"
='Hello'
```

#### Логические значения
```
=true
=false
```

## Архитектура

### Модели данных

```
Spreadsheet (Таблица)
├── sheets (Листы)
│   ├── rows (Строки)
│   │   └── cells (Ячейки)
│   └── column_definitions (Определения колонок)
└── collaborators (Коллабораторы)
```

#### Spreadsheet
- `name` - Название таблицы
- `description` - Описание
- `owner_id` - ID владельца
- `settings` - JSON настройки
- `public` - Публичная/приватная
- `share_token` - Токен для шаринга

#### Sheet
- `name` - Название листа
- `position` - Порядковый номер
- `column_definitions` - JSON массив определений колонок
- `settings` - JSON настройки

#### Row
- `position` - Позиция строки (номер)
- `data` - JSON данные строки

#### Cell
- `column_key` - Ключ колонки (A, B, C, ...)
- `value` - Значение ячейки
- `formula` - Формула (если есть)
- `data_type` - Тип данных
- `metadata` - JSON метаданные

#### Collaborator
- `user_id` или `email` - Идентификатор пользователя
- `permission` - Уровень доступа (view, edit, admin)

### Controllers

#### SpreadsheetsController
- `index` - Список всех таблиц
- `show` - Просмотр таблицы
- `new` / `create` - Создание новой таблицы
- `edit` / `update` - Редактирование
- `destroy` - Удаление

#### SheetsController
- `show` - Просмотр листа
- `create` - Создание листа
- `update` - Обновление
- `destroy` - Удаление
- `add_column` - Добавить колонку
- `remove_column` - Удалить колонку

#### CellsController
- `update` - Обновление ячейки
- `create_row` - Создание строки
- `destroy_row` - Удаление строки

### Services

#### FormulaEvaluator
Сервис для парсинга и вычисления формул:
- Поддержка базовых операций (+, -, *, /)
- Ссылки на ячейки (A1, B2)
- Диапазоны (A1:A10)
- Функции (SUM, AVG, COUNT, MIN, MAX, IF)
- Типизация результатов

### Real-time Collaboration (ActionCable)

#### SpreadsheetChannel
WebSocket канал для real-time обновлений:
- `cell_update` - Обновление ячейки
- `row_added` - Добавлена строка
- `column_added` - Добавлена колонка
- `cursor_position` - Позиция курсора пользователя

## Использование

### Создание таблицы

1. Перейдите на страницу `/spreadsheets`
2. Нажмите "Создать новую таблицу"
3. Заполните название и описание
4. Таблица автоматически создастся с первым листом

### Работа с ячейками

1. **Редактирование**: Кликните на ячейку и начните печатать
2. **Навигация**:
   - `Tab` - Следующая ячейка
   - `Enter` - Ячейка ниже
   - `Shift+Enter` - Многострочный текст
3. **Формулы**: Начните с `=` для ввода формулы
4. **Автосохранение**: Ячейка сохраняется при потере фокуса

### Добавление колонок

1. Нажмите кнопку "+ Добавить колонку"
2. Введите название колонки
3. Выберите тип данных (text, number, date)

### Добавление строк

1. Нажмите кнопку "+ Добавить строку"
2. Или начните печатать в пустой строке внизу

### Работа с листами

1. Переключайтесь между листами через табы внизу
2. Добавляйте новые листы через "+ Новый лист"

### Формулы

1. Выберите ячейку
2. В формульной панели внизу введите формулу
3. Нажмите "Применить"
4. Или начните печатать с `=` прямо в ячейке

## API

### REST API

#### Получить список таблиц
```
GET /spreadsheets
```

#### Создать таблицу
```
POST /spreadsheets
Content-Type: application/json

{
  "spreadsheet": {
    "name": "Моя таблица",
    "description": "Описание",
    "public": false
  }
}
```

#### Обновить ячейку
```
PATCH /spreadsheets/:spreadsheet_id/sheets/:sheet_id/rows/:row_id/cells/:column_key
Content-Type: application/json

{
  "value": "Новое значение",
  "formula": "SUM(A1:A10)",
  "data_type": "number"
}
```

### WebSocket API (ActionCable)

#### Подключение к каналу
```javascript
import { subscribeToSpreadsheet } from 'channels/spreadsheet_channel'

const subscription = subscribeToSpreadsheet(spreadsheetId, sheetId)
```

#### Отправка обновления
```javascript
subscription.sendCellUpdate(rowId, columnKey, value, formula)
```

## Клавиатурные сокращения

- `Tab` - Следующая ячейка (вправо)
- `Shift+Tab` - Предыдущая ячейка (влево)
- `Enter` - Ячейка ниже
- `Shift+Enter` - Многострочный ввод
- `Escape` - Отменить редактирование
- `F2` - Редактировать формулу

## Ограничения и будущие улучшения

### Текущие ограничения

1. Диапазоны формул работают только для одной колонки (A1:A10)
2. Нет поддержки мультиколоночных диапазонов (A1:C10)
3. Нет undo/redo
4. Нет копирования/вставки через clipboard
5. Нет фильтрации и сортировки
6. Нет условного форматирования

### Планируемые улучшения

- [ ] Мультиколоночные диапазоны в формулах
- [ ] Копирование/вставка ячеек
- [ ] Импорт/экспорт CSV, Excel
- [ ] Фильтры и сортировка
- [ ] Условное форматирование ячеек
- [ ] Графики и визуализации
- [ ] Комментарии к ячейкам
- [ ] История изменений (версионирование)
- [ ] Права доступа на уровне листов
- [ ] Mobile-responsive интерфейс
- [ ] Offline mode с синхронизацией
- [ ] Расширенные формулы (VLOOKUP, DATE, TEXT и др.)

## Производительность

- Оптимизировано для таблиц до 1000 строк
- WebSocket обновления происходят в real-time
- Автосохранение с debouncing для снижения нагрузки

## Безопасность

- Валидация данных на клиенте и сервере
- CSRF защита для всех форм
- XSS защита через escape
- Права доступа на уровне таблиц
- Защита от SQL injection через ActiveRecord

## Тестирование

Запустите тесты:

```bash
# Все тесты
bin/rails test

# Тесты моделей
bin/rails test test/models

# Тесты контроллеров
bin/rails test test/controllers

# Тесты сервисов
bin/rails test test/services
```

## Примеры использования

### Пример 1: Учет расходов

1. Создайте таблицу "Бюджет 2025"
2. Добавьте колонки: Дата, Категория, Сумма, Описание
3. Добавьте строки с расходами
4. В конце добавьте формулу: `=SUM(C1:C100)` для подсчета общей суммы

### Пример 2: Инвентаризация роботов

1. Создайте таблицу "Роботы"
2. Колонки: Модель, Серийный номер, Статус, Время работы (часы), Последнее ТО
3. Добавьте формулу для подсчета общего времени работы: `=SUM(D:D)`
4. Поделитесь таблицей с коллегами для совместной работы

### Пример 3: Планирование заданий

1. Создайте таблицу "График заданий"
2. Колонки: Дата, Время, Робот, Оператор, Цель, Статус
3. Используйте для планирования и отслеживания выполнения

## Интеграция с A2D2

Табличный редактор может быть использован для:

- Управления списками роботизированных систем
- Планирования заданий в табличном формате
- Учета технического обслуживания
- Инвентаризации компонентов
- Анализа телеметрических данных
- Ведения журналов инспекций

## Поддержка

При возникновении вопросов или обнаружении багов:
1. Создайте issue в репозитории
2. Опишите проблему максимально подробно
3. Приложите скриншоты при необходимости

## Лицензия

Часть проекта A2D2. Проприетарное ПО.

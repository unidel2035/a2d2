# TypeScript интерфейсы A2D2

Этот каталог содержит TypeScript интерфейсы для системы A2D2, синхронизированные с Rails моделями.

## 📁 Структура файлов

- **`workflow-interfaces.ts`** - Оригинальные интерфейсы n8nDD (справочный материал)
- **`a2d2-interfaces.ts`** - Актуальные интерфейсы A2D2, сгенерированные из Rails схемы БД
- **`README.md`** - Эта документация

## 🎯 Назначение

TypeScript интерфейсы необходимы для:

1. **Frontend разработки** - типизация данных при работе с API
2. **Интеграций** - согласованные типы при обмене данными
3. **Документации** - автоматическая документация структур данных
4. **Валидации** - проверка соответствия данных схеме

## 📝 Основные интерфейсы

### Система Workflow (n8n-compatible)

```typescript
IWorkflow           // Рабочий процесс
IWorkflowNode       // Узел в процессе
IWorkflowConnection // Соединение между узлами
IWorkflowExecution  // Исполнение процесса
```

### Система агентов

```typescript
IAgent              // Интеллектуальный агент
IAgentTask          // Задача агента
ILlmRequest         // Запрос к LLM
ILlmUsageSummary    // Статистика использования LLM
```

### Автоматизация процессов

```typescript
IProcess                // Бизнес-процесс
IProcessStep            // Шаг процесса
IProcessExecution       // Исполнение процесса
IProcessStepExecution   // Исполнение шага
```

### Управление документами

```typescript
IDocument           // Документ
IReport             // Отчет
```

### Интеграции

```typescript
IIntegration        // Интеграция с внешней системой
IIntegrationLog     // Лог операций интеграции
```

### Управление пользователями и роботами

```typescript
IUser               // Пользователь
IRobot              // Робот/агент
ITask               // Задание
IMaintenanceRecord  // Запись ТО
ITelemetryData      // Телеметрия
IInspectionReport   // Отчет инспекции
```

### Аналитика

```typescript
IMetric             // Метрика
IDashboard          // Дашборд
```

### Электронные таблицы

```typescript
ISpreadsheet        // Таблица
ISheet              // Лист
IRow                // Строка
ICell               // Ячейка
ICollaborator       // Соавтор
```

## 🔄 Обновление интерфейсов

### Автоматическая генерация

Для автоматического обновления интерфейсов на основе актуальной схемы БД:

```bash
# Из корня проекта
ruby script/generate_typescript_interfaces.rb
```

Этот скрипт:
1. Читает `db/schema.rb`
2. Парсит определения таблиц и колонок
3. Генерирует TypeScript интерфейсы с правильными типами
4. Сохраняет результат в `experiments/n8ndd-interfaces/a2d2-interfaces.ts`

### Маппинг типов

Rails/PostgreSQL типы автоматически преобразуются в TypeScript:

| Rails тип | TypeScript тип |
|-----------|----------------|
| `integer`, `bigint` | `number` |
| `float`, `decimal` | `number` |
| `string`, `text` | `string` |
| `boolean` | `boolean` |
| `date`, `datetime`, `time` | `string` (ISO формат) |
| `json`, `jsonb` | `Record<string, any>` |

### Nullable поля

- Поля с `null: false` → обязательные (`field: type`)
- Поля без ограничений → опциональные (`field?: type`)

### Default значения

Значения по умолчанию добавляются в комментарии:

```typescript
status: string; // default: inactive
priority: number; // default: 5
```

## 📦 Использование в проектах

### Установка

```bash
# Копировать файл интерфейсов в ваш TypeScript проект
cp experiments/n8ndd-interfaces/a2d2-interfaces.ts src/types/
```

### Импорт

```typescript
import {
  IWorkflow,
  IWorkflowNode,
  IAgent,
  IUser
} from './types/a2d2-interfaces';
```

### Примеры использования

#### Типизация API ответов

```typescript
import { IWorkflow, IPaginatedResponse } from './types/a2d2-interfaces';

async function fetchWorkflows(): Promise<IPaginatedResponse<IWorkflow>> {
  const response = await fetch('/api/workflows');
  return response.json();
}
```

#### Типизация компонентов React

```typescript
import { IAgent } from './types/a2d2-interfaces';

interface AgentCardProps {
  agent: IAgent;
}

export function AgentCard({ agent }: AgentCardProps) {
  return (
    <div>
      <h3>{agent.name}</h3>
      <p>Status: {agent.status}</p>
      <p>Success Rate: {agent.success_rate}%</p>
    </div>
  );
}
```

#### Валидация данных

```typescript
import { IWorkflowNode } from './types/a2d2-interfaces';

function isValidNode(data: any): data is IWorkflowNode {
  return (
    typeof data.id === 'number' &&
    typeof data.name === 'string' &&
    typeof data.node_type === 'string' &&
    typeof data.workflow_id === 'number'
  );
}
```

## 🔍 Соответствие Rails моделям

Каждый интерфейс соответствует Rails модели:

| TypeScript интерфейс | Rails модель | Файл |
|---------------------|--------------|------|
| `IWorkflow` | `Workflow` | `app/models/workflow.rb` |
| `IWorkflowNode` | `WorkflowNode` | `app/models/workflow_node.rb` |
| `IWorkflowConnection` | `WorkflowConnection` | `app/models/workflow_connection.rb` |
| `IAgent` | `Agent` | `app/models/agent.rb` |
| `IAgentTask` | `AgentTask` | `app/models/agent_task.rb` |
| `IUser` | `User` | `app/models/user.rb` |
| ... | ... | ... |

## 🔧 Конфигурация генератора

Скрипт генерации настраивается через константы в `script/generate_typescript_interfaces.rb`:

```ruby
# Приоритетные таблицы для генерации
priority_tables = %w[
  workflows workflow_nodes workflow_connections
  agents agent_tasks
  processes documents reports
  users robots
]

# Маппинг типов
TYPE_MAPPING = {
  'integer' => 'number',
  'string' => 'string',
  'boolean' => 'boolean',
  # ...
}
```

## 📚 Дополнительные материалы

### n8n совместимость

Интерфейсы workflow системы (`IWorkflow`, `IWorkflowNode`, `IWorkflowConnection`) спроектированы для совместимости с форматом n8n. Rails модели включают методы экспорта/импорта:

```ruby
# app/models/workflow.rb
def to_n8n_format
  {
    id: id,
    name: name,
    nodes: workflow_nodes.map(&:to_n8n_format),
    connections: workflow_connections.map(&:to_n8n_format),
    # ...
  }
end

def self.from_n8n_format(data, user: nil)
  # Импорт workflow из n8n формата
end
```

### Enum типы

Для enum полей используются union types:

```typescript
// Вместо: status: string
// Используется:
status: 'active' | 'inactive' | 'error';

// Или отдельный тип:
type WorkflowStatus = 'active' | 'inactive' | 'error';
```

### JSON поля

JSON/JSONB поля типизируются как `Record<string, any>`. Для более точной типизации можно создать специфичные интерфейсы:

```typescript
interface WorkflowSettings {
  timeout?: number;
  retry_on_fail?: boolean;
  error_workflow?: string;
}

interface IWorkflow {
  // ...
  settings?: WorkflowSettings; // Вместо Record<string, any>
}
```

## 🚀 Лучшие практики

1. **Регулярно обновляйте** интерфейсы после изменений в схеме БД
2. **Коммитьте** сгенерированные файлы в репозиторий
3. **Версионируйте** интерфейсы вместе с API версиями
4. **Используйте Type Guards** для runtime валидации
5. **Создавайте специализированные типы** для сложных JSON полей
6. **Документируйте** нестандартные маппинги и бизнес-логику

## 📝 История изменений

### 2025-10-29
- ✅ Создан генератор TypeScript интерфейсов
- ✅ Сгенерированы интерфейсы для всех основных моделей
- ✅ Добавлена документация по использованию
- ✅ Настроена совместимость с n8n форматом

## 🤝 Вклад

При добавлении новых моделей или изменении существующих:

1. Обновите миграции БД
2. Запустите `bin/rails db:migrate`
3. Регенерируйте интерфейсы: `ruby script/generate_typescript_interfaces.rb`
4. Проверьте корректность сгенерированных типов
5. Закоммитьте изменения

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. LICENSE.md для подробной информации.

---

*Документация актуальна на: 2025-10-29*
